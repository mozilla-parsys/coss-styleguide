<div class="feature {{ modifier }}">
    <h1>{{ title }}</h1>
    <p>{{ intro }}</p>
    {% if call_to_action %}
    <a href="{{ call_to_action.url }}">{{ call_to_action.title }}</a>
    {% endif %}
</div>